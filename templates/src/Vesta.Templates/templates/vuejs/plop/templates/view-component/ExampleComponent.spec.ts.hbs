import { mount } from "@vue/test-utils";
import { test, expect } from "vitest";
import {{ ComponentName name }} from "./{{ ComponentName name }}.vue";

test("test view component", async () => {
  expect({{ ComponentName name }}).toBeTruthy();

  const wrapper = mount({{ ComponentName name }}, {
    props: {
      // TODO: add component properties if required
    }
  });
  expect(wrapper.attributes("data-test-id")).toBe(
    "{{ appPrefix }}-{{ viewName }}-{{ name }}-component"
  );
});
